1. ~~Форматирование PDF и HTML с помощью webengine.~~ ✅ готово (`ExportRenderer` + `QWebEnginePage::printToPdf`, ADR 63).

2. Настройки вывода

   

   1. ~~Парсер~~ ✅

   2. ~~Разбивать на страницы и добавлять подписи страниц~~ ✅ (включая «Оставлять номера страниц», ADR 64)

   3. ~~Эксорт pdf (ориентация/поля PDF)~~ ✅ (ADR 64); титульная страница — ещё не сделана

3. Вторая нейронка для проверки.
   
   1. Реестр и скачивание
   2. Опция "проверить автоматически" и кнопка проверки
   3. Проверять: формулы, таблицы, страницы с дубликатами целиком
   4. Подобрать формат запроса, выбрать нейронку

4. Меню для работы - шрифт, изображения, добавлять, убирать блоки

5. Много длинных функций - разбить на логические части. 
   
   1. OcrResult DetTokensParser::parse(const QString &rawText) const - выделить дедупликацию в отдельную функцию