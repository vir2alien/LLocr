1. Проверить весь проект на неиспользуемый код - функции, классы, файлы (например, Q_INVOKABLE QVariantMap estimateModelMemory(const QString &modelPath); в RuntimeController.h), неиспользуемые include. Вывести список всех ненужных включений. Вывести список всех неиспользуемых функций, классов файлов, дать рекомендацию по каждому пункту - можно удалить или ему есть полезное применение в программе. Код не менять.

2. ArchiveExtractor.h, DownloadTask.h, DownloadManager.h, ModelCatalog.h - посмотреть на предмет замены на стороннее приложение. 7zip, hf
   Проверь возможность использования сторонних библиотек и ПО для таких функций, как скачивание моделей (например, утилита hf), распаковка архивов (например, 7zip) и скачивание рантайма (llama.cpp). Насколько это упростит код? Если есть возможность заменить, то как это отразится на лицензии? Цель - сосредоточиться на основных функциях (распознавание, формирование корректного md) и убрать код, который можно заменить сторонними инструментами.

3. Уже есть готовый предпросмотр md. Нужно брать данные для экспорта в doc, pdf, html оттуда.

4. Меню для работы - шрифт, изображения, добавлять, убирать блоки

5. OcrResult DetTokensParser::parse(const QString &rawText) const - выделить дедупликацию в отдельную функцию.

6. Много длинных функций в Runtime - разбить на логические части